extends base

block styles
  link(rel="stylesheet" href="/css/editWorkoutPlan.css")

block content
  .top-bar
    if user.userType === "user"
      a.dashboard-btn(href='/dashboard') Dashboard
    if user.userType === "coach"
      a.dashboard-btn(href='/coachDashboard') Dashboard
    if user.userType === "admin"
      a.dashboard-btn(href='/adminDashboard') Dashboard
    a.back-btn(href='javascript:history.back()') Back

  .page-header
    h1 #{title}
    h2 #{user.username}

  if user.pfpUrl
    img.profile-pic(src=user.pfpUrl alt="Profile Picture")
  else
    img.profile-pic(src="/img/default-user.png" alt="Default Profile")

  p.section-label Select exercises — multiple per muscle allowed

  .target-grid#targetGrid

  form#editWorkoutPlanForm.hidden

  .update-section
    p.form-message#formMessage
    button(type="submit" form="editWorkoutPlanForm") Update Workout Plan

  //- Target / exercise picker modal
  .modal.hidden#targetModal
    .modal-content.large
      .modal-header
        h3#targetTitle
        button#closeTargetModal(type="button" aria-label="Close") ×
      .modal-body
        .exercise-list#targetExerciseList

  //- Exercise info / GIF modal
  .modal.hidden#exerciseModal
    .modal-content
      .modal-header
        span Info
        button#closeModal(type="button" aria-label="Close") ×
      .modal-body
        img#modalGif(alt="Exercise demonstration")
        div#modalInstructions

  script#exercisesData(type="application/json").
    !{JSON.stringify(exercises)}

  script.
    window.exercises = JSON.parse(document.getElementById("exercisesData").textContent)
    window.selectedIds = !{JSON.stringify(selectedIds)}

block scripts
  script(src="/js/editWorkoutPlan.js")
