extends base 
//- base.pug

block styles
    link(rel="stylesheet" href="/css/login.css")
block content
    //- If show navbar, then include navbar
    //- if !hideNavbar 
    //-     include _navbar

    //- REMOVE THIS
    //- a(href='/login') Login
    //- a(href='/signup') Sign Up

   

    form(id='loginForm' novalidate) 
        h1 Login
        // 🔥 Global message area
        p.form-message#formMessage

        label(for='email') Email
        input(type='email' name='email' id='email' required)
        small.form-error#emailError

        label(for='password') Password
        input(type='password' name='password' id='password' required)
        small.form-error#passwordError 

        a(href='/forgotPassword') Forgot password?

        button(type='submit' id='loginBtn')
            span.btn-text Log In
            span.spinner.hidden Loading...

        button(type='button' id='signupBtn') Sign Up
            

    block scripts
        script(src='/js/login.js')
        //- login.js


